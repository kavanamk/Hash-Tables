class Solution(object):
    def smallerNumbersThanCurrent(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        sorted_nums=sorted(nums)
        res=[]
        for num in nums:
            i=sorted_nums.index(num)
            res.append(i)
        return res
